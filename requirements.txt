# --- runtime: detection, depth, graph -------------------------------------
torch>=2.1.0
torchvision>=0.16.0
torch-geometric>=2.5.0
ultralytics>=8.2.0          # YOLOv10 / YOLOv8-World detection
transformers>=4.40.0        # Depth Anything V2 via HF pipeline
opencv-python-headless>=4.9.0
pillow>=10.0.0              # detect_depth.py converts frames to PIL for the HF depth pipeline
numpy>=1.26.0
scipy>=1.12.0
networkx>=3.2               # only for debugging/visualizing graphs, not the runtime path

# --- optional runtime -----------------------------------------------------
pyttsx3>=2.90               # offline TTS for speech_output.py / pipeline.py --speak
